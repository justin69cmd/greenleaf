# Point the app at a backend running on your own machine instead of the
# deployed one. Leave these unset to use the production endpoints.
VITE_API_URL=http://localhost:4000
VITE_WS_URL=ws://localhost:4000
