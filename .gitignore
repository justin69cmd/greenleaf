# Dependencies
node_modules/

# Builds
dist/

# Secrets — never commit API keys or SMTP credentials
.env
server/.env

# Runtime data
server/users.json
server/agent_workspace/
*.log

# Local tooling
.claude/
.DS_Store

# Word lock files
~$*

# Reports (kept local, not published)
report/

# Archives, backups & scratch (never source)
*.zip
*.tgz
*.stitchbak
Claude outputs/
