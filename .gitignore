# Dependencies
node_modules/

# Builds
dist/

# Secrets — never commit API keys or SMTP credentials
.env
.env.local
server/.env

# Runtime data — the customer database and anything migrated into it
server/data/
server/users.json
server/users.json.migrated
server/agent_workspace/
server/agent_runs/
*.log

# Local tooling
.claude/
.DS_Store

# Word lock files
~$*

# Reports (kept local, not published)
report/

# Archives, backups & scratch (never source)
*.zip
*.tgz
*.stitchbak
Claude outputs/
