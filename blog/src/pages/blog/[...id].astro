---
// DEPRECATED: post rendering moved to src/pages/[...id].astro so posts live at
// /blog/<slug> (with base "/blog"). This file is intentionally neutralized —
// it emits no routes — because the folder blocks file deletion. Safe to delete
// manually in Finder if you like.
export function getStaticPaths() {
  return []
}
---
